<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@color/black"
    android:layout_height="match_parent"
    tools:context=".Spash_Screen">

    <TextView
        android:id="@+id/splash_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="CSI VESIT"
        android:textSize="70dp"
        android:padding="10dp"
        android:textStyle="bold"
        android:textColor="@color/white"
        android:background="@drawable/spash_box"
        android:layout_centerInParent="true"
        android:layout_marginBottom="50dp"
        />
    <ProgressBar
        android:id="@+id/splash_progress"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:max="100"
        android:layout_below="@id/splash_text"
        android:layout_marginTop="50dp"
        android:layout_centerHorizontal="true"
        android:outlineSpotShadowColor="@color/colorPrimaryDark"
        android:outlineAmbientShadowColor="@color/colorPrimaryDark"

        />

    <TextView
        android:id="@+id/textView2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your portal to anything and everything CSI-VESIT"
        android:layout_below="@id/splash_progress"
        android:layout_centerHorizontal="true"
        android:padding="4dp"
        android:layout_marginTop="20dp"
        android:textSize="23sp"
        android:textColor="@color/white"
        />

</RelativeLayout>